# Singly linked lists

## Description

Implementation of a singly linked list with functions to print, count, add, and free list nodes.

## Key concepts

- Singly linked list node structure (`n`, `next`)
- Traversing and printing list elements
- Counting nodes in a list
- Adding nodes at the beginning and at the end
- Freeing all nodes safely
