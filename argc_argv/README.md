# argc_argv

## Description

Small programs that use command-line arguments with `argc` and `argv` to read and process user input from the shell.

## Key concepts

- Argument count (`argc`) and argument vector (`argv`)
- Validating and converting string arguments to integers
- Basic error handling for missing or invalid arguments
