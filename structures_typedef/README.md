# Structures and typedef

## Description

Projects that use `struct` and `typedef` to define custom data types and related helper functions.

## Key concepts

- Defining and using `struct` types
- Using `typedef` to create shorter type names
- Initializing, printing, allocating, and freeing struct instances
- Encapsulating data and behavior for a simple `dog` structure
